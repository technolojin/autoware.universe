name: SensingA.pipeline

depends:
  - SensorA.module

nodes:
  - node: sensor_a
    element: SensorA.module

external_interfaces:
  input: []
  output:
    - name: pointcloud

connections:
  - from: sensor_a.output.pointcloud
    to: output.pointcloud

parameters: []

configurations:
  - name: sensor_a.composable_node
    to: sensor_a.configuration.composable_node
